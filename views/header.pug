mixin navbar-fixed-mod(name,image,id,style,href)
  - var style = (typeof style === 'undefined') ? "default" : style
  nav( role="navigation", class=["navbar","navbar-fixed-top","navbar-" + style] )
    .container
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#' + id, aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
        a.navbar-brand(href=href)
          <a class="navbar-brand" href="http://bridgesymphony.com"><img src="images/logo.png" alt="Bridge Symphony">
      .collapse.navbar-collapse( id=id )
        ul.nav.navbar-nav
          block




mixin header
  +navbar-fixed-mod("Bridge Symphony","images/logo.png","navbar")

    if active=='home'
      +nav_item("/","active") Home
      +nav_item("About") About
      +nav_item("Contact") Contact
      +nav_item("Order") Order

    else if active  =='about'
      +nav_item("/") Home
      +nav_item("About","active") About
      +nav_item("Contact") Contact
      +nav_item("Order") Order


    if active=='contact'
      +nav_item("/") Home
      +nav_item("About") About
      +nav_item("Contact","active") Contact
      +nav_item("Order") Order

    else if active  =='order'
      +nav_item("/") Home
      +nav_item("About") About
      +nav_item("Contact") Contact
      +nav_item("Order","active") Order
